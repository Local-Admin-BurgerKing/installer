Sicherheit

Im Hinterkopf behalten -------------------

1 key hat 10 kombinationen

2 keys haben 10²

3 keys 10³

Name + key = unendlich viele  kombinationen da string unendlich lang
name kann aber bekannt sein
womit es nur mehr 10

ein zahlen kode mit
1 Stelle hat 10 kombis
2 Stellen hat 100 -> 10²
3 Stellen hat 1000 -> 10³

2 codes mit 3 stellen haben 10³*10³ -> 10⁶


remember : ein zahlen kode mit 3 Stellen hat 1000 kombinationen
ein key mit 1-3 stellen hingegen hat 1110 kombinationen (dazu kommt dann noch +1 in der hochzahl bei mehreren keys)
-> zudem weiß der hacker das wahrscheinlich nicht und würde dazu gezwungen sein einen vieeel größeren bereich zu scannen wenn wir zB. sagen wir machen 20-50 würde er wahrscheinlich 10-200 scannen (was effektiv 10-50 ist da er das ergebnis dann schon hätte)
Damit ist es eine billige methode. da man damit entweder ohne die verlängerung der keys die kombinationen erhöhen kann oder anstatt von 1 stellen auf 2 zu gehen von 1 auf 1-2 gehen was +10 zuätzliche kombinationen ergibt
(!Funktioniert nur bei String keys)
------------------------------------------


Was dazu führt dass :
Lizensschlüssel besteht aus
Name des besitzers
3 1-2KB Schlüsseln 

Session keys:
sind an die IP gebunden
bestehen aus 50-70 zeichen die aber auch sonderzeichen beinhalten und sind maximal 1 Stunde gültig + Server neustart setzt sie auch zurück


ZUSÄTZLICH:
Sollte eine request mit einem Falschen key oder einer falschen lizens gemacht werden ist die bei lizensen der name und bei session keys die IP für 60 sekunden gesperrt damit sollte bruteforce unmöglich sein das gleiche gilt für passwörter nur dass hier das delay allgemein (auch bei richtigen anfragen) 1 sekunden beträgt da time cheking unmöglich gemacht werden soll. Benutzer maximal 5

Leider sind damit passwörter die größte schwachstelle

XSS VERHINDERN!

Login einmalcode*?
